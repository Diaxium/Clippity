{
  "name": "@clippity/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared IPC wire-format contracts between the Clippity frontend and the Tauri backend.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "check": "tsc --noEmit",
    "lint": "eslint .",
    "format": "prettier --write \"src/**/*.ts\""
  }
}
