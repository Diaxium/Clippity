/**
 * Home feature — public surface.
 *
 * Only `HomeLayout` is exported. The dashboard mounts it as the `home`
 * view (the default landing view of the main window).
 */

export { HomeLayout } from "./components/HomeLayout";
